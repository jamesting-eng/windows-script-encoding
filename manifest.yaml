name: windows-script-encoding
version: 1.0.4
description: "Windows script (.ps1/.bat/.cmd) encoding & line-ending iron rules — stop PowerShell parse-stage failures from recurring (CRLF + pure ASCII, self-check before run)"
category: dev-tools
author: jamesting-eng
license: MIT
homepage: https://github.com/jamesting-eng/windows-script-encoding
repository: https://github.com/jamesting-eng/windows-script-encoding
keywords:
  - windows
  - powershell
  - encoding
  - crlf
  - ascii
  - script
  - workbuddy
platforms:
  - windows
requirements:
  - Windows 10/11
  - PowerShell 5.1+ (Windows PowerShell) or PowerShell 7+
  - Python 3.10+ (for safe file writing)
